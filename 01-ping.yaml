- name: ping the server # in ansible we difine list with "-"
  hosts: web # list of hosts your ansible wants to connet
  tasks: # list of tasks /modules/collections
  - name:
    ansible.builtin.ping: