- name: loops demo
  hosts: local
  connections: local
  tasks:
  - name: print names
    ansible.builtin.debug:
      msg: "Hello {{ item }}"
    loop:
    - prashanth
    - venkatesh
    - ravikumar
    