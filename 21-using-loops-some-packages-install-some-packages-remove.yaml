- name: loops demo
  hosts: web
  become: yes
  tasks:
  - name: install some packes and remove the some packages
    ansible.builtin.package:
      name: "{{ item.name }}"
      state: "{{ item.state }}"
    loop:
    - { name: 'mysql', state: 'present'}
    - { name: 'nginx', state: 'absent' }
    - { name: 'postfix', state: 'present' }
    - { name: 'httpd', state: 'absent' }